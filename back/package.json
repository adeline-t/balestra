{
  "name": "balestra-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply balestra-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply balestra-db"
  },
  "devDependencies": {
    "wrangler": "^4.7.0"
  }
}
